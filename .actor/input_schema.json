{
    "$schema": "https://apify.com/schemas/v1/input.ide.json",
    "title": "Instagram Caption Keyword Search",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "keyword": {
            "title": "Keyword",
            "type": "string",
            "description": "The word or phrase to search for inside Instagram post captions. Examples: \"stanley\", \"very happy\". The keyword is also used (with spaces and punctuation removed) as a hashtag to find candidate posts.",
            "editor": "textfield",
            "prefill": "stanley"
        },
        "maxPosts": {
            "title": "Max matching posts",
            "type": "integer",
            "description": "Maximum number of matching posts to return. The Actor scrapes more candidates than this and keeps only those whose caption contains the keyword.",
            "default": 50,
            "minimum": 1,
            "maximum": 1000
        },
        "sortBy": {
            "title": "Sort by date",
            "type": "string",
            "description": "Order of the returned posts by publish date.",
            "editor": "select",
            "default": "newest",
            "enum": ["newest", "oldest"],
            "enumTitles": ["Newest first (most recent → oldest)", "Oldest first (oldest → most recent)"]
        },
        "minLikes": {
            "title": "Minimum likes",
            "type": "integer",
            "description": "Only keep posts with at least this many likes. Use 0 for no minimum. Posts with hidden like counts are treated as 0.",
            "default": 0,
            "minimum": 0,
            "unit": "likes"
        },
        "minComments": {
            "title": "Minimum comments",
            "type": "integer",
            "description": "Only keep posts with at least this many comments. Use 0 for no minimum. Posts with hidden comment counts are treated as 0.",
            "default": 0,
            "minimum": 0,
            "unit": "comments"
        },
        "postedAfter": {
            "title": "Posted after (from date)",
            "type": "string",
            "description": "Only keep posts published on or after this date. Accepts an absolute date (e.g. 2024-01-31) or a relative value (e.g. \"7 days\", \"2 weeks\", \"1 month\", \"1 year\"). Leave empty for no lower bound.",
            "editor": "datepicker"
        },
        "postedBefore": {
            "title": "Posted before (to date)",
            "type": "string",
            "description": "Only keep posts published on or before this date. Accepts an absolute date (e.g. 2024-12-31) or a relative value (e.g. \"7 days\"). Leave empty for no upper bound.",
            "editor": "datepicker"
        },
        "location": {
            "title": "Location",
            "type": "string",
            "description": "Only keep posts tied to this place, e.g. a neighborhood, city, or venue (\"Leblon\", \"Jardim Botânico\"). A post matches if the place appears in its Instagram location tag OR is mentioned in the caption. Accent- and case-insensitive. Leave empty for no location filter.",
            "editor": "textfield"
        },
        "enablePoliticalAnalysis": {
            "title": "Enable AI political analysis",
            "type": "boolean",
            "description": "When ON, an AI (Groq) checks whether each post's caption is polemic/political. For polemic posts, it scrapes the most-liked comments and summarizes public sentiment (% positive / negative / neutral) and the core issue. This adds AI calls and an extra comment-scraping step.",
            "default": false,
            "groupCaption": "AI political analysis (optional)",
            "groupDescription": "Turn a caption search into a political-sentiment scanner."
        },
        "groqApiKey": {
            "title": "Groq API key",
            "type": "string",
            "description": "Your free Groq API key (https://console.groq.com). Required when 'Enable AI political analysis' is ON. Can also be supplied via the GROQ_API_KEY environment variable.",
            "editor": "textfield",
            "isSecret": true
        },
        "groqModel": {
            "title": "Groq model",
            "type": "string",
            "description": "Groq chat model used for the analysis.",
            "editor": "textfield",
            "default": "llama-3.3-70b-versatile"
        },
        "maxComments": {
            "title": "Max comments per post",
            "type": "integer",
            "description": "How many of the most-liked comments to scrape and analyze per polemic post (only used when AI analysis is ON).",
            "default": 30,
            "minimum": 1,
            "maximum": 500,
            "unit": "comments"
        }
    },
    "required": ["keyword"]
}
